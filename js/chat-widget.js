/* ============================================================
   GADURA REAL ESTATE — Chat Widget — DISABLED
   The floating chat-bubble popup was removed at the owner's request to leave a
   single, clean WhatsApp button (injected by conversion-widgets.js). This stub
   is a no-op so existing <script src="/js/chat-widget.js"> tags keep loading
   without error. Restore from git history if the chat popup is wanted again.
   ============================================================ */
(function () { /* intentionally empty — widget disabled */ })();
